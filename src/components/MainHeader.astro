<header class="main-header has-cover">
  <div class="content">
    <slot />
    <div class="main-cover">
      <img src="/src/assets/MainCover/IMG_2313.jpg" />
    </div>
  </div>
</header>

<style>
  .main-header {
    padding-top: calc(10rem + 4vw);
    position: relative;
  }
  .has-cover {
    padding-top: calc(15rem + 8vw);
    background: black;
  }
  .main-cover {
    position: absolute;
    width: 100%;
    height: 100%;
    left: 0;
    top: 0;
    z-index: 5;
    background: no-repeat scroll center center / cover #000;
    animation: 0.5s ease-in forwards fadein;
    display: flex;
    align-items: center;
  }

  .main-cover::after {
    content: "";
    display: block;
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 60%;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0) 0%,
      rgba(0, 0, 0, 0.333) 100%
    );
  }
  .main-cover img {
    position: relative;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
</style>
